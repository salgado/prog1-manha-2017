#include <stdio.h>

int main()
{
  // declara variaveis
  int x;
  int y;
  int soma;
  // poderia ser int x, y, soma;
  
  //atribui valores aos parametros da soma
  x = 10;
  y = 5;
  
  //calcula soma
  soma = x + y;
  
  //imprime a soma
  printf("\nResultado da soma=%d\n", soma);
  
  return 0;
}